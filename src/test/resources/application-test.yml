spring:
  config:
    activate:
      on-profile: "test"

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/RecordIt
    username: sa
    password:
  h2:
    console:
      enabled: true

  redis:
    host: localhost
    port: 63790
  session:
    store-type: redis